{
  "name": "@maxmusic/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Shared formatters, validators and domain constants for the MaxMusic platform panels. TypeScript source exports — consumed via Next.js transpilePackages, no build step.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
