'use strict';
// TODO: Phase 1 — fields: name, slug (immutable pre-save hook), mode (managed|autonomous),
//                 status (pending|active|suspended|terminated), branding { schoolName, logoUrl, primaryColor, tagline },
//                 ownerTeacherId, contactEmail, rent { amount, billingCycle }, tokenVersion,
//                 activatedAt, suspendedAt, terminatedAt
module.exports = {};
