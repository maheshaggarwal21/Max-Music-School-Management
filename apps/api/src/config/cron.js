'use strict';

// Phase 7 — node-cron: daily joinStatus advance, validity expiry, rent-due flagging.

function init() {
  console.warn('[cron] not initialised (Phase 7)');
}

module.exports = { init };
