'use strict';
// TODO: Phase 2 — auditLog({ institutionId, actorId, actorRole, actorName, impersonatedBy,
//                             action, entityType, entityId, entityLabel, changes, before, after, ip })
//                  Uses { w: 0 } — never blocks. Strips: password, passwordHash, recoveryOtp, totpSecret, jwt_token.
module.exports = {};
