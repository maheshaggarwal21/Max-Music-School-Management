'use strict';

// Phase 7 — Nodemailer with per-institution branded sender.
// Stubbed so Phase 2-4 controllers can import without crashing.

async function sendMail() {
  console.warn('[mailer] not implemented (Phase 7)');
  return { messageId: null, accepted: [] };
}

module.exports = { sendMail };
