'use strict';
// TODO: Phase 2 — accept god-token in instAuth; bypass scopeGuard + panelGuard
//                 stamp actorRole: 'superadmin' + impersonatedBy: operatorId on every auditLog call
module.exports = (req, res, next) => next();
